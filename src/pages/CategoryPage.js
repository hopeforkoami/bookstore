import React from 'react';
import Header from '../Components/Header';

const CategoryPage = () => (
  <div className="container">
    <Header />
    <section className="section">
      <button className="category_button" type="button">Check status</button>
    </section>

  </div>
);

export default CategoryPage;
